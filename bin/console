#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "ukrsib_api"
require "date"
require "./spec/environment"
# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

API_TOKEN_NAME = "pb-token"
client_id = ENV["UKRSIB_CLIENT_ID"]
client_secret = ENV["UKRSIB_CLIENT_SECRET"]
client_code = ENV["UKRSIB_CLIENT_CODE"]
@client = UkrsibAPI::Client.new(client_id:, client_secret:, client_code:, private_key:)

# @today = Date.today
# @last_month = @today.prev_month
# @recent = Time.now - (10 * 24 * 3600)

require "irb"
binding.irb # rubocop:disable Lint/Debugger
